{
	"name": "root",
	"type": "module",
	"scripts": {
		"build": "pnpm --parallel build",
		"dev": "pnpm --parallel dev",
		"lint": "pnpm biome lint --write packages/*/src/**/*.ts",
		"format": "pnpm biome format --write packages/*/src/**/*.ts",
		"check": "pnpm biome check --write packages/*/src/**/*.ts"
	},
	"devDependencies": {
		"@types/node": "^22.10.5",
		"prettier": "^3.3.3"
	},
	"publishConfig": {
		"access": "public"
	}
}
